<div class="ui segment">
    <table class="ui celled table">
        <thead>
            <tr>
                <th>Data</th>
                <th>Contenuti</th>
            </tr>
        </thead>
        <tbody>
            <% pageData.forEach((notification) => { 
                    let date = new Date(notification.time);
                %>
            <tr>
                <td class="collapsing"><%= date.toLocaleString('it-IT', { timeZone: 'UTC' }) %></td>
                <td><%= notification.content %></td>
            </tr>
            <% }) %>
        </tbody>
    </table>
</div>